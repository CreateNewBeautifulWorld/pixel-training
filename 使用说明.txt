# Uint8 量化神经网络 - 完整包

## 📦 包含文件

```
uint8_neural_network.zip
├── QUICKSTART.md              ← 👈 先看这个！快速入门指南
├── README.md                  ← 详细技术文档
├── train_simple.py            ← Python训练脚本（numpy版）
├── inference_uint8.cpp        ← C++推理引擎
├── uint8_nn_rtl.v             ← Verilog RTL参考设计
├── Makefile                   ← 编译脚本
├── train_data.txt             ← 训练数据示例（1000样本）
├── weights_uint8.bin          ← 已训练权重（二进制）
└── weights_uint8.bin.txt      ← 已训练权重（文本，可读）
```

## 🚀 3步快速开始

### 步骤1: 解压
```bash
unzip uint8_neural_network.zip
cd uint8_neural_network/
```

### 步骤2: 训练（可选，已有训练好的权重）
```bash
# 需要先安装numpy
pip install numpy

# 运行训练
python train_simple.py
```

### 步骤3: C++推理
```bash
# 编译
make

# 运行
./inference_uint8
```

## 💻 系统要求

### Python训练:
- Python 3.6+
- numpy

### C++推理:
- g++ (支持C++11)
- Linux/Mac自带，Windows需要MinGW

### RTL仿真（可选）:
- Icarus Verilog / ModelSim / Vivado

## 📊 预期输出

运行 `./inference_uint8` 应该看到:

```
=== Uint8 量化神经网络推理引擎 ===
权重加载成功!
FC1: [16, 48]
FC2: [3, 16]

--- 单样本测试 ---
预测类别: 1

--- 测试集评估 ---
总样本数: 1000
正确数: 1000
准确率: 100%
```

## 🎯 网络架构

```
输入: 16 pixels × RGB = 48 个 uint8
  ↓
Layer 1: 全连接 48→16 + ReLU + 量化
  ↓
隐藏层: 16 个 uint8
  ↓
Layer 2: 全连接 16→3
  ↓
输出: 3分类 (0/1/2)
```

## 📝 文件说明

### 核心代码：
- **train_simple.py**: numpy实现的训练，不需要PyTorch
- **inference_uint8.cpp**: C++推理引擎，读取权重并执行推理
- **uint8_nn_rtl.v**: Verilog RTL设计，可综合到FPGA/ASIC

### 数据文件：
- **train_data.txt**: CSV格式，每行=48个像素值+1个标签
- **weights_uint8.bin**: 二进制权重，C++读取
- **weights_uint8.bin.txt**: 文本权重，方便查看/调试

### 文档：
- **QUICKSTART.md**: 快速入门，5分钟上手
- **README.md**: 完整技术文档，包含优化建议

## 🔧 自定义修改

### 改变输入尺寸（例如4个像素）：
```python
# train_simple.py
model = SimpleNN(input_dim=12, hidden_dim=8, output_dim=3)
```

```cpp
// inference_uint8.cpp
static const int INPUT_DIM = 12;  // 4 pixels × 3 channels
static const int HIDDEN_DIM = 8;
```

### 改变类别数（例如5分类）：
```python
model = SimpleNN(input_dim=48, hidden_dim=16, output_dim=5)
```

```cpp
static const int OUTPUT_DIM = 5;
```

### 使用自己的数据：
创建 CSV 文件，格式：
```
pixel0_r,pixel0_g,pixel0_b,...,pixel15_b,label
128,64,32,...,255,0
64,128,255,...,128,1
...
```

## ⚠️ 常见问题

**Q: 编译时找不到g++**
```bash
# Ubuntu/Debian
sudo apt-get install g++

# Mac
xcode-select --install

# Windows
下载安装 MinGW: https://sourceforge.net/projects/mingw/
```

**Q: Python找不到numpy**
```bash
pip install numpy
# 或
pip3 install numpy
```

**Q: 权重文件找不到**
```bash
# 确保weights_uint8.bin和可执行文件在同一目录
ls -l weights_uint8.bin
ls -l inference_uint8
```

**Q: 想用PyTorch训练（更快）**
```bash
# 需要额外下载 train_uint8_nn.py（PyTorch版）
pip install torch
python train_uint8_nn.py
```

## 📚 学习路径

1. ✅ **运行示例** - 5分钟
   - 解压 → make → ./inference_uint8

2. 📖 **理解原理** - 30分钟
   - 阅读 QUICKSTART.md
   - 查看 train_simple.py 代码

3. 🔧 **修改网络** - 1小时
   - 改变网络结构
   - 用自己的数据训练

4. 💻 **RTL实现** - 3-5小时
   - 理解 uint8_nn_rtl.v
   - 仿真验证
   - FPGA综合

## 🌟 特色

✅ **硬件友好**: 全uint8量化，适合FPGA/ASIC  
✅ **轻量级**: 总权重仅835字节  
✅ **易理解**: 仅2层网络，代码简洁  
✅ **完整流程**: 训练→推理→RTL全覆盖  
✅ **开箱即用**: 包含训练好的权重  

## 📞 获取帮助

- 查看 README.md 获取详细文档
- 查看 QUICKSTART.md 获取快速指南
- 代码中有详细注释

## 🎓 适用场景

- 学习神经网络量化
- 嵌入式AI应用
- FPGA/ASIC加速器设计
- 边缘计算
- IoT设备

---

**版本**: 1.0  
**更新**: 2025-01-16  
**许可**: MIT License

祝你学习愉快！🚀
