# 项目文件结构

```
uint8_neural_network/
│
├── 📘 文档
│   ├── QUICKSTART.md              ← 👈 从这里开始！
│   ├── README.md                  ← 完整技术文档
│   └── 使用说明.txt               ← 解压后先看这个
│
├── 🐍 Python训练
│   └── train_simple.py            ← numpy实现，不需要PyTorch
│                                    输出: weights_uint8.bin
│
├── ⚙️ C++推理
│   ├── inference_uint8.cpp        ← 推理引擎源码
│   └── Makefile                   ← 编译脚本
│                                    编译: make
│                                    运行: ./inference_uint8
│
├── 🔧 RTL硬件
│   └── uint8_nn_rtl.v             ← Verilog设计
│                                    可综合到FPGA/ASIC
│
└── 📊 数据/权重
    ├── train_data.txt             ← 训练样本（1000条）
    ├── weights_uint8.bin          ← 二进制权重（C++读取）
    └── weights_uint8.bin.txt      ← 文本权重（可读）

```

## 📊 文件大小

```
train_simple.py          8.5 KB   ← Python训练脚本
inference_uint8.cpp      7.4 KB   ← C++推理引擎
uint8_nn_rtl.v           8.2 KB   ← Verilog RTL
train_data.txt         170.0 KB   ← 训练数据
weights_uint8.bin        0.9 KB   ← 权重（二进制）
weights_uint8.bin.txt    2.9 KB   ← 权重（文本）
QUICKSTART.md            6.6 KB   ← 快速指南
README.md                7.0 KB   ← 详细文档
Makefile                 0.2 KB   ← 编译脚本
───────────────────────────────
总计                   ~210 KB
压缩后                  ~81 KB   ← zip文件
```

## 🔄 工作流程

```
第1步: 训练（Python）
┌─────────────────────────┐
│ python train_simple.py  │
└─────────────────────────┘
           ↓
    生成权重文件
           ↓
┌─────────────────────────┐
│ weights_uint8.bin       │
│ weights_uint8.bin.txt   │
└─────────────────────────┘


第2步: 推理（C++）
┌─────────────────────────┐
│ make                    │
│ ./inference_uint8       │
└─────────────────────────┘
           ↓
    读取权重，执行推理
           ↓
┌─────────────────────────┐
│ 预测类别: 0/1/2         │
│ 准确率: 100%            │
└─────────────────────────┘


第3步: 硬件（RTL）
┌─────────────────────────┐
│ uint8_nn_rtl.v          │
└─────────────────────────┘
           ↓
    综合到FPGA/ASIC
           ↓
┌─────────────────────────┐
│ 硬件加速器              │
└─────────────────────────┘
```

## 🎯 关键数字

| 指标 | 值 |
|------|-----|
| **网络结构** | 48 → 16 → 3 |
| **参数量** | 835 bytes |
| **输入** | 16像素×RGB = 48 uint8 |
| **输出** | 3分类 (0/1/2) |
| **精度** | uint8 (8-bit) |
| **训练样本** | 1000 |
| **准确率** | 100% |
| **MAC运算** | 832次 |
| **推理延迟** | ~1μs (C++) |
| **功耗** | ~1mW (RTL估算) |

## 🚀 快速命令

```bash
# 解压
unzip uint8_neural_network.zip
cd uint8_neural_network/

# 安装依赖
pip install numpy

# 训练（可选，已有权重）
python train_simple.py

# 编译C++
make

# 运行推理
./inference_uint8

# 清理
make clean
```

## 📝 数据格式

### 输入数据 (train_data.txt)
```
# 格式: pixel0_r,pixel0_g,pixel0_b,...,pixel15_b,label
128,64,32,255,128,0,...,64,128,255,0
64,128,255,32,64,128,...,255,0,128,1
...
```

### 权重文件 (weights_uint8.bin.txt)
```
# FC1 Weight (16x48)
94,89,75,92,68,77,99,...
212,197,205,200,201,...
...

# FC1 Bias (16)
65,213,225,3,40,...

# FC2 Weight (3x16)
...

# FC2 Bias (3)
...
```

## 💡 修改建议

### 改输入尺寸
```python
# 4像素 = 12输入
model = SimpleNN(input_dim=12, hidden_dim=8, output_dim=3)
```

### 改隐藏层
```python
# 更大的网络
model = SimpleNN(input_dim=48, hidden_dim=32, output_dim=3)
```

### 改分类数
```python
# 5分类
model = SimpleNN(input_dim=48, hidden_dim=16, output_dim=5)
```

---

**提示**: 所有修改都需要重新训练并重新编译C++代码！
